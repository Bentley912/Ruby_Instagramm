.col-md-6.col-md-offset-3
  #pics.transitons-enabled
    - @pics.each do |pic|
      .box.panel.panel-default
        =link_to (image_tag pic.image.url(:medium)), pic
        %h3= link_to pic.title, pic
